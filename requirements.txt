# Wireless Diagnostic Suite - Python dependencies
# Install with:  pip3 install -r requirements.txt
#
# Uses minimum-version (>=) constraints rather than hard pins so pip can
# fetch prebuilt wheels matching whatever Python version is installed
# (3.9 through 3.13+). Hard pins caused source-build failures on Python 3.13.
#
# NOTE: pyobjc frameworks (CoreWLAN) are macOS-only. This tool runs on macOS.

# --- WiFi scanning (macOS CoreWLAN bridge) ---
pyobjc>=10.0

# --- Internet speed testing ---
speedtest-cli>=2.1

# --- Plotting / heatmaps ---
matplotlib>=3.8
numpy>=1.26
scipy>=1.11
Pillow>=10.0

# --- PDF report generation ---
reportlab>=4.0

# --- Interactive offline viewer & survey heatmaps ---
plotly>=5.18

# --- Machine learning (quality scoring & anomaly detection) ---
scikit-learn>=1.4
joblib>=1.3
